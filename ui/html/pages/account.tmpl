{{define "title"}}Account{{end}}

{{define "main"}}
<section class='hero hero--compact'>
    <div>
        <h2>Account</h2>
    </div>
</section>

{{if .Form}}
{{with .Form}}
<section class='panel account-panel'>
    <div class='account-layout'>
        <div class='account-card account-card--details'>
            <div class='account-card__header'>
                <p class='account-card__eyebrow'>Profile details</p>
                <h3 style="margin:0; font-size:1.45rem;">{{.Name}}</h3>
            </div>

            <div class='info-list__row'>
                <span>Name</span>
                <strong>{{.Name}}</strong>
            </div>

            <div class='info-list__row'>
                <span>Email</span>
                <strong>{{.Email}}</strong>
            </div>

            <div class='info-list__row'>
                <span>Joined</span>
                <strong>{{humanDate .Created}}</strong>
            </div>

            <div class='info-list__row'>
                <span>Password</span>
                <strong><a href='/password/update'>Change password</a></strong>
            </div>
        </div>

        <aside class='account-card account-card--avatar'>
            <h3 class='account-card__title'>Avatar</h3>

            <div class='account-avatar-frame'>
                <div class='account-avatar-shell'>
                    <img class='account-avatar-image' src="{{if .AvatarURL}}{{.AvatarURL}}{{else}}/static/img/avatars/penguin.png{{end}}" alt="{{.Name}} avatar" onerror="this.onerror=null; this.src='/static/img/avatars/penguin.png';">
                </div>
            </div>

            <form action="/account/avatar" method="post" enctype="multipart/form-data" class='account-avatar-form' data-avatar-form>
                <input type='hidden' name='csrf_token' value='{{$.CSRFToken}}'>
                <label class='account-avatar-form__label' for="avatar">JPG or PNG</label>
                <input class='account-avatar-form__input' id="avatar" type="file" name="avatar" accept="image/png, image/jpeg" required>
                <p class='account-avatar-form__hint' data-avatar-hint>Choose a JPG or PNG before saving.</p>
                <button class='account-avatar-form__button' type="submit" data-avatar-submit hidden disabled>Save avatar</button>
            </form>
        </aside>
    </div>
</section>
{{end}}
{{end}}

{{end}}
